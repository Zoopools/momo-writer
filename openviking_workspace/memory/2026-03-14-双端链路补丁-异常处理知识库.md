# 双端链路补丁 - 异常处理知识库

**时间**: 2026-03-14 19:43 GMT+8  
**来源**: 哥哥双端链路补丁指令  
**状态**: 全境就绪，随时待命

---

## 1. 仓库身份对齐 (Identity Resolution)

### 真实仓库信息
- **仓库名**: `momo-writer`
- **权限**: Private（私有仓库）
- **URL**: `https://github.com/Zoopools/momo-writer.git`

### 异常记录
**问题**: Repository not found 伪装报错

**触发条件**:
- 家端（MBP）未完成全域授权
- 首次克隆或推送时

**根本原因**:
Private 仓库在未授权时会伪装成 "Repository not found"，而非 "Permission denied"

**处理方案**:
✅ **已解决**: 哥哥已在双端手动同步 `scripts/sync-memory.sh`
- 避开了初始化的权限死循环
- 脚本已可直接使用

**备用方案**:
若遇到权限问题:
```bash
# 方案 A: 使用 SSH 方式
git remote set-url origin git@github.com:Zoopools/momo-writer.git

# 方案 B: 使用 GitHub Token
# 在 ~/.gitconfig 或仓库中配置
git config --global credential.helper osxkeychain

# 方案 C: GitHub Desktop 手动鉴权
# 打开 GitHub Desktop，完成一次手动同步
```

---

## 2. 脚本状态确认 (Script Status)

### 脚本信息
| 属性 | 值 |
|------|-----|
| **位置** | `~/Documents/openclaw/openviking_workspace/scripts/sync-memory.sh` |
| **权限** | ✅ Executable (chmod +x) |
| **版本** | GOLDEN v1.0 |

### 运行环境要求
**必须在**: `openviking_workspace` 根目录下执行

**正确执行**:
```bash
cd ~/Documents/openclaw/openviking_workspace
./scripts/sync-memory.sh to      # 同步到公司
./scripts/sync-memory.sh from    # 从公司同步
./scripts/sync-memory.sh both    # 双向同步
```

**严禁**:
```bash
# ❌ 错误：在 ~ 目录下越权操作
cd ~
./Documents/openclaw/openviking_workspace/scripts/sync-memory.sh

# ❌ 错误：使用绝对路径直接执行
~/Documents/openclaw/openviking_workspace/scripts/sync-memory.sh
```

---

## 3. 运维红线补充

### 红线 1: 路径容错
**墨墨执行脚本前，必须先执行 cd 切换工作目录**

**正确流程**:
```bash
# 1. 切换工作目录
cd ~/Documents/openclaw/openviking_workspace

# 2. 验证当前目录
pwd
# 输出: /Users/wh1ko/Documents/openclaw/openviking_workspace

# 3. 执行同步脚本
./scripts/sync-memory.sh to
```

**错误示例**:
```bash
# ❌ 未切换目录直接执行
./scripts/sync-memory.sh to
# 结果: 路径错误，同步失败
```

### 红线 2: 认证兜底
**若 sync-memory.sh 报错 Authentication failed**

**墨墨需立即执行**:
1. **停止操作**
2. **提示哥哥**:
```
哥哥，同步脚本遇到认证错误：

错误信息: Authentication failed

可能原因:
1. GitHub Token 过期
2. 未配置 SSH 密钥
3. 首次使用需要手动鉴权

解决方案:
选项 A: 检查 GitHub Token
  - 访问 https://github.com/settings/tokens
  - 确认 token 有 repo 权限

选项 B: 使用 GitHub Desktop 完成一次手动鉴权
  - 打开 GitHub Desktop
  - 克隆 momo-writer 仓库
  - 完成一次手动同步

选项 C: 配置 SSH 密钥
  - ssh-keygen -t ed25519 -C "your@email.com"
  - 将公钥添加到 GitHub

请哥哥选择一种方式处理，处理完成后通知墨墨继续同步。🖤
```

---

## 4. 结论：全境状态 (Final Conclusion)

### 公司端 (Mac-mini)
| 组件 | 状态 | 详情 |
|------|------|------|
| AGFS Server | 🟢 监听中 | PID: 65229, 127.0.0.1:1933 |
| Git 仓库 | 🟢 已推 | momo-writer, Private |
| Workspace | 🟢 就绪 | /Users/whiteareas/Documents/openclaw/openviking_workspace |

### 家里端 (MBP)
| 组件 | 状态 | 详情 |
|------|------|------|
| 脚本 | 🟢 已造 | sync-memory.sh, Executable |
| 路径 | 🟢 已通 | openviking_workspace |
| 状态 | 🟢 待命 | 随时可执行同步 |

### 双端链路状态
```
家里 MBP                    公司 Mac-mini
    |                            |
    |  scripts/sync-memory.sh    |
    |--------------------------->|
    |                            |
    |  Git 同步 (Private Repo)   |
    |<-------------------------->|
    |                            |
    v                            v
~/Documents/openclaw/      ~/Documents/openclaw/
    └── openviking_workspace/    └── openviking_workspace/
        ├── 40 记忆文件              ├── (同步目标)
        └── scripts/                 └── (同步目标)

状态: 🟢 全境就绪，随时待命
```

---

## 5. 墨墨的异常处理速查表

| 异常 | 症状 | 处理方案 |
|------|------|---------|
| Repository not found | 克隆/推送时 | 检查权限，使用 GitHub Desktop 鉴权 |
| Authentication failed | sync-memory.sh 报错 | 提示哥哥检查 Token 或 SSH |
| Path not found | 脚本执行失败 | 确认 cd 到 openviking_workspace |
| Permission denied | 脚本无法执行 | chmod +x scripts/sync-memory.sh |
| Connection refused | 1933 端口不通 | 检查 AGFS Server 是否运行 |

---

## 6. 同步操作标准流程

```bash
# Step 1: 切换工作目录
cd ~/Documents/openclaw/openviking_workspace

# Step 2: 检查本地变更
git status

# Step 3: 提交本地变更
git add .
git commit -m "sync: 更新记忆文件"

# Step 4: 推送到远程
git push

# Step 5: 同步到 OpenViking workspace
./scripts/sync-memory.sh to

# Step 6: 验证同步结果
ssh company-mac-mini "ls ~/Documents/openclaw/openviking_workspace/ | wc -l"

# Step 7: 报告哥哥
# "哥哥，同步完成！家里 X 个文件已推送到公司端。🖤"
```

---

**墨墨记录**: 双端链路补丁已更新，异常处理知识库已完善。全境就绪，随时待命。🖤

**签名**: 墨墨 (Mò) - 中央指挥部  
**时间**: 2026-03-14 19:43
