# 墨墨恢复指南 - MoMo Recovery Guide

**备份日期:** 2026-03-05  
**备份名称:** momo-2026-03-05  
**备份位置:** 
- 本地：`~/Documents/openclaw/backups/momo-2026-03-05/`
- 云端：https://github.com/Zoopools/momo-backups

---

## 🚀 快速恢复（5 分钟）

### 场景 1：新电脑/系统重装

```bash
# 1. 安装 OpenClaw
npm install -g openclaw

# 2. 克隆备份
git clone git@github.com:Zoopools/momo-backups.git ~/Documents/openclaw/backups/momo-2026-03-05

# 3. 恢复工作区
cp -r ~/Documents/openclaw/backups/momo-2026-03-05/workspace/* ~/Documents/openclaw/agents/writer/

# 4. 恢复配置
cp ~/Documents/openclaw/backups/momo-2026-03-05/openclaw/openclaw.json ~/.openclaw/

# 5. 重启 Gateway
openclaw gateway restart
```

### 场景 2：配置损坏/误删

```bash
# 恢复工作区
cd ~/Documents/openclaw/backups/momo-2026-03-05/workspace
git checkout .  # 如果有本地 git

# 或者从备份恢复
cp -r ~/Documents/openclaw/backups/momo-2026-03-05/workspace/* ~/Documents/openclaw/agents/writer/
```

---

## 📦 完整恢复步骤

### 步骤 1：准备工作

```bash
# 检查备份是否存在
ls -la ~/Documents/openclaw/backups/momo-2026-03-05/

# 如果没有本地备份，从云端克隆
git clone git@github.com:Zoopools/momo-backups.git ~/Documents/openclaw/backups/momo-2026-03-05
```

### 步骤 2：恢复工作区

```bash
# 备份当前状态（可选，防止覆盖）
cp -r ~/Documents/openclaw/agents/writer ~/Documents/openclaw/agents/writer.backup.$(date +%Y%m%d)

# 恢复工作区文件
cp -r ~/Documents/openclaw/backups/momo-2026-03-05/workspace/* ~/Documents/openclaw/agents/writer/
```

### 步骤 3：恢复 OpenClaw 配置

```bash
# 恢复配置
cp ~/Documents/openclaw/backups/momo-2026-03-05/openclaw/openclaw.json ~/.openclaw/

# 如果有 .env 文件（包含 API 密钥）
cp ~/Documents/openclaw/backups/momo-2026-03-05/openclaw/.env ~/.openclaw/ 2>/dev/null || echo "无 .env 文件"
```

### 步骤 4：恢复记忆

```bash
# 恢复记忆数据库
cp -r ~/Documents/openclaw/backups/momo-2026-03-05/memory/* ~/Documents/openclaw/agents/writer/memory/
```

### 步骤 5：恢复技能（可选）

技能目录太大，建议单独处理：

```bash
# 方案 A：重新安装技能
# 从 claw123.ai 或 GitHub 重新安装需要的技能

# 方案 B：从旧位置复制
# 如果有 ~/.openclaw/skills 备份
cp -r ~/.openclaw/skills.backup/* ~/.openclaw/skills/
```

### 步骤 6：重启服务

```bash
# 重启 Gateway
openclaw gateway restart

# 检查状态
openclaw status
```

---

## 🔍 验证恢复

```bash
# 1. 检查工作区文件
ls ~/Documents/openclaw/agents/writer/*.md

# 2. 检查配置
cat ~/.openclaw/openclaw.json | python3 -c "import sys,json; print(json.load(sys.stdin)['agents']['defaults']['model']['primary'])"

# 3. 检查记忆
ls ~/Documents/openclaw/agents/writer/memory/

# 4. 测试对话
# 在飞书或网页聊天中发送"你好"
```

---

## 📋 恢复清单

- [ ] 工作区文件恢复（SOUL.md, USER.md, IDENTITY.md）
- [ ] OpenClaw 配置恢复（openclaw.json）
- [ ] 记忆恢复（memory/）
- [ ] 技能恢复（skills/）
- [ ] Gateway 重启
- [ ] 验证对话正常

---

## 🆘 故障排除

### 问题 1：GitHub 认证失败

```bash
# 生成 SSH key
ssh-keygen -t ed25519 -C "your-email@example.com" -f ~/.ssh/id_ed25519 -N ""

# 添加到 GitHub
cat ~/.ssh/id_ed25519.pub
# 复制输出，添加到 https://github.com/settings/keys

# 测试连接
ssh -T git@github.com
```

### 问题 2：权限错误

```bash
# 修复权限
chmod -R 700 ~/Documents/openclaw/
chmod -R 700 ~/.openclaw/
```

### 问题 3：Gateway 无法启动

```bash
# 查看日志
openclaw logs --follow

# 重新配置
openclaw configure
```

---

## 📞 联系墨墨

如果恢复遇到问题：

1. 检查备份是否完整
2. 查看故障排除部分
3. 重新执行恢复步骤

---

**最后更新:** 2026-03-05  
**墨墨版本:** 1.0  
**备份仓库:** Zoopools/momo-backups

🖤 墨墨会一直在这里等你！
